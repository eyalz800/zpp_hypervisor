# The SHA-256 of out/release/x86_64/zpp_hypervisor built with
# ZPP_NESTED_VMX=OFF, one line per compiler version string.
#
# Read scripts/ci/check-nested-absent.sh for why this exists. The short
# version: while nested VMX is being built, the switch-off binary is not
# supposed to move, so any change to it should be a deliberate and stated act
# rather than something noticed later on a machine that stopped booting.
#
# Updating a line is fine. Updating it without saying in the commit message
# what shared code changed and why is not - that is the whole point.
#
# The format is the compiler's `--version` first line, two spaces, the hash.
# Keyed on the compiler because the hash is as much a property of the toolchain
# as of the source; a version with no line here is skipped with a warning.
Homebrew clang version 22.1.1  9ce9f163c5564609b73ec3b296f64be13e303b2ea3398954c08b4fae91f5a91b
